{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		// Package-specific settings
		"useDefineForClassFields": false, // TODO: Fix the 7 issues so this setting can be dropped
		"strict": false // TODO: Fix all TypeScript issues so strict can be true
	},
	"include": ["src"],
  "references": [{ "path": "./tsconfig.vite.json" }]
}
