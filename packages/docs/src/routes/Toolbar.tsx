import ConfigPage from '../components/ConfigPage'

export default function Toolbar() {
	return (
		<ConfigPage title="Toolbar" overview="The optional toolbar can be customized." tag="toolbar" />
	)
}
