import ConfigPage from '../components/ConfigPage'

export default function Zoombar() {
	return (
		<ConfigPage
			title="Zoom Bar"
			overview="Carbon Charts can be configured to display a Zoom Bar above that provides drag handles permitting the user to focus on a particular portion of the x-axis data."
			tag="zoombar"
		/>
	)
}
