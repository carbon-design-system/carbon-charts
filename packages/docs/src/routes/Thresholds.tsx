import ConfigPage from '../components/ConfigPage'

export default function Thresholds() {
	return (
		<ConfigPage
			title="Thresholds"
			overview="Thresholds can be added to charts with axes to communicate targets or limits."
			tag="thresholds"
		/>
	)
}
