import ConfigPage from '../components/ConfigPage'

export default function Truncation() {
	return (
		<ConfigPage
			title="Truncation"
			overview="Carbon Charts automatically truncate long label names in axes and tooltips by adding an ellipsis."
			tag="truncation"
		/>
	)
}
